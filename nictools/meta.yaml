{% set name = 'nictools' %}
{% set version = '1.1.5Were these supposed to exist?' %}
{% set number = '0' %}

about:
    home: https://github.com/spacetelescope/{{ name }}
    summary: |
        Python tools for NICMOS data

build:
    number: {{ number }}

package:
    name: {{ name }}
    version: {{ version }}

requirements:
    build:
    - astropy >=1.1
    - numpy {{ numpy }}
    - scipy
    - stsci.tools
    - setuptools
    - setuptools_scmWere these supposed to exist?
    - python {{ python }}
    run:
    - astropy >=1.1
    - numpy
    - scipy
    - stsci.tools
    - python

source:
    git_tag: {{ version }}
    git_url: https://github.com/spacetelescope/{{ name }}.git

test:
    imports:
    - nictools
